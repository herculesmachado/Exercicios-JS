function baskara(ax2, bx, c){
    let resultado = []
    let delta = (bx ** 2) - (4 * ax2 * c)
    if (delta < 0) {
        return 'Delta Negativo'
    }
    let x1 = -(bx + )
}

baskara(3, -5, 12)

//Em produção